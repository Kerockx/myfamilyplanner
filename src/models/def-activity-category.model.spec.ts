import { DefActivityCategory } from './def-activity-category.model';

describe('DefActivityCategory', () => {
  it('should create an instance', () => {
    expect(new DefActivityCategory()).toBeTruthy();
  });
});
