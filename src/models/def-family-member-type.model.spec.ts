import { DefFamilyMemberType } from './def-family-member-type.model';

describe('DefFamilyMemberType', () => {
  it('should create an instance', () => {
    expect(new DefFamilyMemberType()).toBeTruthy();
  });
});
