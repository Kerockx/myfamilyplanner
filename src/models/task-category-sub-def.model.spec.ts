import { TaskCategorySubDef } from './task-category-sub-def.model';

describe('TaskCategorySubDef', () => {
  it('should create an instance', () => {
    expect(new TaskCategorySubDef()).toBeTruthy();
  });
});
