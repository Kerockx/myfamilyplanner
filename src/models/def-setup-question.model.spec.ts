import { DefSetupQuestion } from './def-setup-question.model';

describe('DefSetupQuestion', () => {
  it('should create an instance', () => {
    expect(new DefSetupQuestion()).toBeTruthy();
  });
});
