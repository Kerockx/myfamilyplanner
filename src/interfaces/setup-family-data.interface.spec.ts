import { SetupFamilyData } from './setup-family-data.interface';

describe('SetupFamilyData', () => {
  it('should create an instance', () => {
    expect(new SetupFamilyData()).toBeTruthy();
  });
});
